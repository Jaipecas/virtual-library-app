
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#5282d1',
      light: '#c6dbfc',
      dark: '#366ec9',
    },
    secondary: {
      main: '#a066e3',
      light: '#d5beee',
      dark: '#8f46e3',
    },
  },
});

export default theme;
